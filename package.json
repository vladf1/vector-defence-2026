{
  "name": "vector-defence-browser",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate:audio": "node scripts/generate-audio-assets.mjs",
    "render:towers": "node scripts/render-towers.mjs",
    "build": "tsc --noEmit && vite build",
    "build:pages": "tsc --noEmit && vite build --base=/vector-defence-2026/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "playwright": "^1.59.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.10"
  }
}
